// Wrapper around `supabase gen types typescript --linked`. Implemented in Fase 1.
async function main() {
  console.log("[generate-types] Use: supabase gen types typescript --linked > packages/db/types.ts");
}
main();
